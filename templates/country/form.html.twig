{% extends 'base.html.twig' %}

{% block title %}Liste des Pays{% endblock %}

{% block body %}
    <div class="container mt-3 text-center">
        <div class="row">
            <div class="col">
                <u><h1 class="m-5">Ajouter un Pays</h1></u>
                <div class="text-center">{{ form_start(form) }} </div>
                {{ form_widget(form) }}
                    <button class="btn btn-danger text-center my-3">Enregister</button>
                {{ form_end(form) }}

            </div>
        </div>
    </div>
{% endblock %}